This program push the buttons on the printer. It works on the STM32F103 MCU, 
and written in C using HAL in AC6 OpenStm32.

Format of the message: delay:BUTTONS:BUTTONS:...:BUTTONS. Where delay - time 
between clicking, BUTTONS - bitmask of buttons. 1 if the button is pressed 0
 otherwise, if clicking is skipped, BUTTONS is equal 0.
